<%= form_with(model: current_user.active_relationships.build) do |f| %>
  <%= hidden_field_tag :followee_id, @user.id %>
  <%= button_tag(type: "submit", class: "follow-button btn p-2") do %>
    <i class="fas fa-lg fa-user-plus pr-2"></i>フォローする
  <% end %>
<% end %>
