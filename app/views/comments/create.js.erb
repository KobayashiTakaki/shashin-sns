$("#post-<%= @post.id %> .comments ul")
  .append("<%= escape_javascript(render 'comments/comment', comment: @created_comment) %>");
console.log("<%= escape_javascript(render 'comments/comment', comment: @created_comment) %>");
