<%= form_with(model: current_user.likes.build, url: articles_like_path) do |f| %>
  <%= hidden_field_tag :article_id, article.id %>
  <%= button_tag(type: "submit", class: "like-button") do %>
    <i class="far fa-2x fa-heart"></i>
  <% end %>
<% end %>
