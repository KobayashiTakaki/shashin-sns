<%= form_with(model: current_user.likes.build, url: articles_unlike_path) do |f| %>
  <%= hidden_field_tag :article_id, article.id %>
  <%= button_tag(type: "submit", class: "unlike-button") do %>
    <i class="fas fa-2x fa-heart"></i>
  <% end %>
<% end %>
