<%= form_with(model: current_user.likes.build, url: posts_unlike_path) do |f| %>
  <%= hidden_field_tag :post_id, post.id %>
  <%= button_tag(type: "submit", class: "unlike-button") do %>
    <i class="fas fa-2x fa-heart"></i>
  <% end %>
<% end %>
